#ifndef GUARD_mean_h
// Mean function header file
//If any special types (string of vector) were used in this function they would have to be #include'd
  #define GUARD_mean_h
  float get_mean (int nums[],int num_count);
#endif
